// Configurazione Supabase — incolla qui i valori del TUO progetto Supabase.
// Li trovi su https://supabase.com/dashboard → il tuo progetto → Settings → API.
// La "anon key" è pubblica per design (la sicurezza sta nelle Row Level Security
// policies definite in setup.sql), quindi può stare in un repo pubblico.
// Se i valori sono vuoti, il sito parte in MODALITÀ DEMO con dati di esempio.
window.MERCATINO_CONFIG = {
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: ""
};
